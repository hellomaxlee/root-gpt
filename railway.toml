[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn tools.run:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
